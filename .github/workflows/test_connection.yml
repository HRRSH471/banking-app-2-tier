name : Test Connection For Piplines
on:
  workflow_call:
  
jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Message
        run: echo "GitHub Actions is working successfully ✅"